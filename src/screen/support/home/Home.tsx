import { Box, TextField } from '@mui/material';
import React from 'react';

const Home = () => {
  return (
    <Box bgcolor="Background" p={2}>
      <TextField fullWidth />
    </Box>
  );
};

export default Home;
